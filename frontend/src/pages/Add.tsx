import { AddForm } from "../components/Expenses";

const Add = () => {
  return (
    <section className="p-4 sm:ml-64 flex flex-col gap-3 ">
      <AddForm />
    </section>
  );
};

export default Add;
